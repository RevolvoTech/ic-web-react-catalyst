# Server-only Catalyst backend origin. Do not expose this as a NEXT_PUBLIC_* value.
# Leave it unset only when intentionally using the website's local QGIS fixture.
CATALYST_BACKEND_URL=http://127.0.0.1:4000
